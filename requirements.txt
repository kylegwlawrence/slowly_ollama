# Direct dependencies for ollama_slowly.
# Pinned to the versions resolved on Python 3.13.13 during Phase 1.
# Transitive deps are intentionally left unpinned — for a simple local app,
# that's a readability/maintainability win; switch to a full `pip freeze`
# lockfile later if reproducibility bites.

# Web framework + ASGI server (uvicorn[standard] pulls in uvloop, httptools,
# watchfiles, websockets — the "production-ish" extras).
fastapi==0.136.1
uvicorn[standard]==0.47.0

# HTTP client for talking to the local Ollama server (supports streaming).
httpx==0.28.1

# Server-side templating for the HTMX frontend.
jinja2==3.1.6

# .env file loading. The standard, focused library for this job; no
# transitive deps. Reads .env at the project root into os.environ at
# app startup so the rest of the code reads via os.environ.get(...).
python-dotenv==1.2.2

# Testing.
pytest==9.0.3
pytest-asyncio==1.3.0
